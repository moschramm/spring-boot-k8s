apiVersion: v1
kind: Service
metadata:
  name: spring-boot-demo
  namespace: demo-app
  labels:
    app.kubernetes.io/name: spring-boot-demo
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: spring-boot-demo

